import type { ContactConfig } from '@/types/contact';
import { CONTACT_PAGE_CONTENT } from './content';

export const CONTACT_CONTENT: ContactConfig = {
  CONTACT_PAGE: CONTACT_PAGE_CONTENT
};

// Re-export for direct import
export { CONTACT_PAGE_CONTENT };